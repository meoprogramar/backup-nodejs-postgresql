# backup-nodejs-postgresql
Script para realizar backup com nodejs em banco de dados PostgreSQL.

Para funcionar é só alterar o arquivo **.env** com as configurações do seu BD, em seguida rodar
`node backup.js`
